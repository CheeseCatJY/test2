name: Download and Export Vosk
on: [workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Pull Real Vosk Image
        run: docker pull alphacephei/vosk-websocket:latest

      - name: Save Image to Tar
        run: docker save -o vosk-server.tar alphacephei/vosk-websocket:latest

      - name: Upload Tar Artifact
        uses: actions/upload-artifact@v4
        with:
          name: vosk-server-pure
          path: vosk-server.tar
